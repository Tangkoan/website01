4. គឺរៀបចំ Layout Header , Sidebar, Content
    - path: 
            - admin_livewire\resources\views\components\header.blade.php
            - admin_livewire\resources\views\components\sidebar.blade.php


* កែ Dark : Light Mode ដោយយើងបានប្រើ Livewire កុំអោយ Refresh page ពេលនេះគឺបានសម្រេចហើយ

app.blade.php
    <script>
        function applyTheme() {
            if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
                document.documentElement.classList.add('dark')
            } else {
                document.documentElement.classList.remove('dark')
            }
        }

        // រត់នៅពេល Load ទំព័រដំបូង
        applyTheme();

        // រត់រាល់ពេល Livewire ប្តូរទំព័រ (wire:navigate)
        document.addEventListener('livewire:navigated', applyTheme);
    </script>